/* Add your own Gemini API key here (or in Settings — each user's key is saved
   on their own device and survives refresh).
   Option (FREE): Google Gemini — get a free key at https://aistudio.google.com/apikey
   Detection uses Gemini first; if no key is set it falls back to the
   offline on-device model. Overridable from the app settings. */
var GEMINI_KEY = "";
var GEMINI_MODEL = "gemini-3.6-flash";